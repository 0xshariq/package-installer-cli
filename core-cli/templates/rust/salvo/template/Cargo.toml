[package]
name = "template"
version = "0.1.0"
edition = "2024"

[dependencies]
salvo = { version = "0.68", features = ["tokio", "serve-static"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"